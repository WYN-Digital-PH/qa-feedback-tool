# `NODE_ENV=production` in a shell makes npm omit devDependencies, which strips
# vite, vitest and the rest of the toolchain out of node_modules on every install
# and leaves `npm run dev` failing with "'vite' is not recognized". Installs here are
# for development; the deploy pipeline builds from its own environment.
include=dev
